<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dozycoffee.infrastructure.persistance.mapper.BranchCodeSequenceMapper">

    <insert id="initializeIfAbsent">
        INSERT INTO branch_code_sequence (year, next_seq)
        VALUES (#{year}, 1)
        ON DUPLICATE KEY UPDATE year = year
    </insert>

    <!-- FOR UPDATE로 락을 잡은 뒤 Java에서 UPDATE → SELECT 순으로 호출 -->
    <select id="lockAndGetSeq" resultType="int">
        SELECT next_seq
        FROM branch_code_sequence
        WHERE year = #{year}
        FOR UPDATE
    </select>

    <update id="incrementSeq">
        UPDATE branch_code_sequence
        SET next_seq = next_seq + 1
        WHERE year = #{year}
    </update>

</mapper>
